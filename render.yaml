services:
  - type: web
    name: tse-assistance-booking
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: APEX_USERNAME
        sync: false
      - key: APEX_PASSWORD
        sync: false
